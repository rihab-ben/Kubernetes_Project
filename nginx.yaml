---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: web
  name: myapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
      - image: imaneaabouche/myapp:v1
        name: myapp

---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: web
  name: myapp
spec:
  ports:
  - nodePort: 32227
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: web
  type: NodePort
